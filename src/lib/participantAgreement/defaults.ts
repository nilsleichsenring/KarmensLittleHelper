import type {
  AgreementLanguage,
  AgreementSectionId,
} from "./types";

export const DEFAULT_AGREEMENT_VERSION = 1;

export const DEFAULT_AGREEMENT_LANGUAGE: AgreementLanguage = "en";

export const DEFAULT_OPTIONAL_SECTIONS: AgreementSectionId[] = [];